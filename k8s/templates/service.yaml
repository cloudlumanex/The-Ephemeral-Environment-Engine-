apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: NAMESPACE_PLACEHOLDER
  labels:
    app: demo-app
    pr-number: "PR_NUMBER_PLACEHOLDER"
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: demo-app
